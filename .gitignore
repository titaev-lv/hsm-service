# Binaries (in root and cmd directories, not source files)
/hsm-service
/cmd/hsm-admin/hsm-admin
*.exe
*.dll
*.so
*.dylib
build/

# Test binaries
*.test
*.out

# Go workspace file
go.work

# Private keys (NEVER commit!)
*.key
*.pem
*.crt
*.srl
!*.example.key
inventory.yaml
revoked.yaml
!revoked.yaml  # Keep default empty revocation list

# SoftHSM tokens
data/tokens/*
!data/tokens/.gitkeep

# Dynamic metadata (managed by automation, not by Git)
metadata.yaml
metadata.yaml.backup-*
metadata-test.yaml
config-test.yaml

# Environment files
.env
.env.local

# Test docker-compose (generated during testing)
docker-compose-test.yml
revoked-test.yaml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
/var/log/
logs/

# Temporary files
tmp/
temp/
kek-metadata.json
